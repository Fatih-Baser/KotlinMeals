<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    
    <data>
        <variable
            name="listener"
            type="com.fatihbaser.kotlinmeals.adapter.MealClickListener" />
        <variable
            name="meals"
            type="com.fatihbaser.kotlinmeals.model.Meal" />
        
    </data>
    
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:onClick="@{listener::onMelaClicked}"
        android:orientation="horizontal">


        <TextView
            android:id="@+id/mealUuidText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:text="@{String.valueOf(meals.uuid)}"/>
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:padding="3dp"
            android:downloadUrl="@{meals.resim}"
            android:src="@mipmap/ic_launcher_round"
            >

        </ImageView>
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:layout_weight="3"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{meals.mealName}"
                android:textSize="20sp"
                android:textStyle="bold"
                android:padding="5dp">


            </TextView>

        </LinearLayout>

    </LinearLayout>
</layout>
